# <project> — CLAUDE.md

Per-project context. Loads only when working in this folder. Keep it about *this*
project; business-wide rules live in the repo-root `CLAUDE.md`.

- **What it is:** <one-liner>
- **Stack:** <languages / frameworks / hosting>
- **Build lives in:** `mvp/` (or sibling clone: see repo-root `.repos.json`)
- **Local conventions:** <anything an agent should know before editing here>
- **Canonical state:** `STATUS.md` (where we are), `brief.md` (immutable),
  `decisions/` (per-project ADRs).
- **Privacy:** client PII stays out of git; `client.md` is a working description only.
