# <project> — client

A **working description** only. Keep PII (full contact dumps, IDs, contracts,
credentials) **out of git** — reference where it lives instead. A real client-data
store + viewer is a later build.

- Who they are (working description):
- Primary contact (handle/first name only):
- What they're trying to do:
- Sensitivities / things to be careful about:
- Where the real data lives (pointer, not the data):
