# inbox

Fallback landing spot for `/capture` when the right home isn't obvious yet.
Not a system to maintain — keep it near-empty. Promote items into the right
project / `decisions/` / `leads/` and delete them from here.
